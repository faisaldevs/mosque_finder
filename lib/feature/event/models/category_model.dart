import 'package:flutter/material.dart';

class Category {
  final String id;
  final String label;
  final String emoji;
  final Color color;

  const Category(this.id, this.label, this.emoji, this.color);
}
